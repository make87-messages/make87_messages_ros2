syntax = "proto3";

package make87_messages_ros2.jazzy.delphi_srr_msgs.msg;

import "make87_messages_ros2/jazzy/std_msgs/msg/header.proto";

message SrrTrack {
     make87_messages_ros2.jazzy.std_msgs.msg.Header header = 1;
     uint32 can_tx_detect_valid_level = 2;
     bool can_tx_detect_status = 3;
     float can_tx_detect_range_rate = 4;
     float can_tx_detect_range = 5;
     float can_tx_detect_angle = 6;
     float can_tx_detect_amplitude = 7;
}
