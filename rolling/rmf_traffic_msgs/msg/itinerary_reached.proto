syntax = "proto3";

package make87_messages_ros2.rolling.rmf_traffic_msgs.msg;


message ItineraryReached {
     uint64 participant = 1;
     uint64 plan = 2;
    repeated uint64 reached_checkpoints = 3;
     uint64 progress_version = 4;
}
