syntax = "proto3";

package make87_messages_ros2.rolling.rmf_traffic_msgs.msg;


message Timespan {
    repeated string maps = 1;
     bool has_lower_bound = 2;
     int64 lower_bound = 3;
     bool has_upper_bound = 4;
     int64 upper_bound = 5;
}
