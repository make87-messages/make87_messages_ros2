syntax = "proto3";

package make87_messages_ros2.rolling.py_trees_ros_interfaces.srv;


message OpenBlackboardStreamRequest {
    repeated string variables = 1;
     bool filter_on_visited_path = 2;
     bool with_activity_stream = 3;
}

message OpenBlackboardStreamResponse {
     string topic = 1;
}
