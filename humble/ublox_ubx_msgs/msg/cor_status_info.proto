syntax = "proto3";

package make87_messages_ros2.humble.ublox_ubx_msgs.msg;

import "make87_messages/core/header.proto";

message CorStatusInfo {
    make87_messages.core.Header header = 1;
     uint32 protocol = 2;
     uint32 err_status = 3;
     uint32 msg_used = 4;
     uint32 correction_id = 5;
     bool msg_type_valid = 6;
     bool msg_sub_type_valid = 7;
     bool msg_input_handle = 8;
     uint32 msg_encrypted = 9;
     uint32 msg_decrypted = 10;
}
