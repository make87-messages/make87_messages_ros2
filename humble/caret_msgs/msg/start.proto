syntax = "proto3";

package make87_messages_ros2.humble.caret_msgs.msg;

import "make87_messages/core/header.proto";

message Start {
    make87_messages.core.Header header = 1;
     uint32 recording_frequency = 2;
     string ignore_nodes = 3;
     string ignore_topics = 4;
     string select_nodes = 5;
     string select_topics = 6;
}
