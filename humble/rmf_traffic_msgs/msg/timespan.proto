syntax = "proto3";

package make87_messages_ros2.humble.rmf_traffic_msgs.msg;

import "make87_messages/core/header.proto";

message Timespan {
    make87_messages.core.Header header = 1;
    repeated string maps = 2;
     bool has_lower_bound = 3;
     int64 lower_bound = 4;
     bool has_upper_bound = 5;
     int64 upper_bound = 6;
}
