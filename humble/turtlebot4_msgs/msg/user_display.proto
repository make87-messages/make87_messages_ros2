syntax = "proto3";

package make87_messages_ros2.humble.turtlebot4_msgs.msg;

import "make87_messages/core/header.proto";

message UserDisplay {
    make87_messages.core.Header header = 1;
     string ip = 2;
     string battery = 3;
    repeated string entries = 4;
     int32 selected_entry = 5;
}
